[pytest]
# Hold lokal pytest rask og stabil som standard.
# Kjør coverage eksplisitt ved behov, f.eks.:
#   .\.venv\Scripts\python.exe -m pytest --cov=. --cov-report=term-missing --cov-fail-under=10
addopts = -q

testpaths =
    tests
